# Example Configuration
# ---------------------
# Do not include directly, instead configure as needed before including this role.
---
cron:
  jobs:
    - name: magento
      schedule: * * * * *
      user: www-data
      command: cd /application && bin/magento cron